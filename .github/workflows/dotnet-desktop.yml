name: Build & Release NativeAOT .NET 9

on:
  push:
    tags:
      - 'v*'   # Triggers on version tags like v1.0.0
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.displayName }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - runtime: win-x86
            displayName: Windows-x86
            os: windows-latest
            ext: .exe
          - runtime: linux-x64
            displayName: Linux-x64
            os: ubuntu-latest
            ext: ''
          - runtime: osx-x64
            displayName: OSX-x64
            os: macos-latest
            ext: ''

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Clean
        run: dotnet clean

      - name: Publish with NativeAOT
        shell: bash
        run: |
          dotnet publish S7Patcher.csproj \
            -c Release \
            -r ${{ matrix.runtime }} \
            -p:PublishAot=true \
            -p:SelfContained=true \
            -p:PublishSingleFile=true \
            -p:StripSymbols=true \
            -o ./publish/${{ matrix.runtime }}

      - name: Compress executable
        shell: bash
        run: |
          cd ./publish/${{ matrix.runtime }}
          zip -9 -r ../../${{ matrix.displayName }}.zip ./*${{ matrix.ext }}

      - name: Upload compressed artifact
        uses: actions/upload-artifact@v4
        with:
          name: nativeaot-${{ matrix.displayName }}
          path: ${{ matrix.displayName }}.zip

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Extract version name
        id: get_version
        run: echo "version_name=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
        # Removes leading 'v' from tag (e.g. v1.9.0.0 -> 1.9.0.0)

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ steps.get_version.outputs.version_name }}
          tag_name: ${{ github.ref_name }}
          body: "Latest release."
          draft: true
          prerelease: false
          files: |
            ./artifacts/nativeaot-Windows-x86/Windows-x86.zip
            ./artifacts/nativeaot-Linux-x64/Linux-x64.zip
            ./artifacts/nativeaot-OSX-x64/OSX-x64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
